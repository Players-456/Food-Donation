# Food Donation Project

This repository contains a food donation application with a React frontend and Express backend.

- `client/`: React frontend application
- `server/`: Node.js + Express backend

## Deployment

Deploy the backend to a Node.js hosting provider and the frontend as a static app. Use MongoDB Atlas for the database.
